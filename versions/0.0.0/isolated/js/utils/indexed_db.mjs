"use strict";const DB_NAME="BlockGameTempDb";export const STORE_NAME="files";const DB_VERSION=1;export async function open_db(){return await new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=_event=>{const db=request.result;if(!db.objectStoreNames.contains(STORE_NAME)){db.createObjectStore(STORE_NAME)}};request.onsuccess=()=>{resolve(request.result)};request.onerror=()=>{reject(request.error)}})}
