"use strict";import*as cbor from"cbor";export class BinaryUuid{static ALPHABET="0123456789ABCDEFGHIJKLMNOPQRSTUV";buf;bytes;constructor(base_32){this.buf=new ArrayBuffer(16);this.bytes=new Uint8Array(this.buf);const clean_str=base_32.toUpperCase();let bigint_val=0n;for(const char of clean_str){const val=BinaryUuid.ALPHABET.indexOf(char);if(val===-1){throw new Error("Invalid base-32 string.")}bigint_val=bigint_val<<5n|BigInt(val)}for(let i=15;i>=0;i--){this.bytes[i]=Number(bigint_val&0xFFn);bigint_val>>=8n}}}const CBOR_BINARY_UUID_EXTENSION={Class:BinaryUuid,tag:37,encode(instance,encode){const byte_string=instance.buf;encode(byte_string)},decode(buf){const arr=new Uint8Array(buf);let bigint_val=0n;for(const byte of arr){bigint_val<<=8n;bigint_val|=BigInt(byte)}return bigint_val.toString(32).padStart(26,"0")}};cbor.addExtension(CBOR_BINARY_UUID_EXTENSION);
